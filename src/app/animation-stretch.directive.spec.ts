import { AnimationStretchDirective } from './animation-stretch.directive';

describe('AnimationStretchDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationStretchDirective();
    expect(directive).toBeTruthy();
  });
});
